{
  "name": "police-and-thief",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "selftest": "npm run build && node dist/tools/selftest.js"
  },
  "devDependencies": {
    "@types/node": "^22.13.0",
    "typescript": "^5.6.3"
  }
}
